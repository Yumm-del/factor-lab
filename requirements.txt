# factor-lab 依赖（全部国内可装、无编译问题）
# 刻意保持精简：因子计算用 pandas/numpy 向量化，不引入 scipy/alphalens 等重依赖
numpy>=2.0
pandas>=2.0
baostock>=0.8.8
streamlit>=1.40
openai>=1.40
python-dotenv>=1.0
plotly>=5.20
# 以下仅文档/图表生成需要（scripts/ 下的 make_charts.py、PDF 管线）
matplotlib>=3.8
pypdf>=4.0
